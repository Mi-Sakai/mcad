# M8を始める前に — v0.7.X 系列のロードマップ

作成: 2026-07-26(v0.7.2 リリース直後)
改定: 2026-08-01 — 「M8前に決める設計入力」(第6章)と「製図規定・図面様式規定の方針」(第7章)を追加。M8前に決めるものが単位系1件から拡大した
位置づけ: **v0.7.X 系列(M7完了後〜M8着手前)のロードマップ**。M8「出力と設定永続化」へ進む前に、実使用で痛点を洗い出す価値がどれだけあるかを判断するための材料であり、v0.7.x の番号外リリースで何を拾うかの指針でもある。

マイルストーン単位の全体設計は [`DESIGN.md`](./DESIGN.md) 第7章が正。本ファイルはその第7章「随時対応(マイルストーン外)」を実使用の観点から具体化したもので、**M8 の詳細設計を DESIGN.md へ追記した時点で役目を終える**(そのとき単位系の扱いが M8 のタスクとして確定するため)。

## この系列で完了したもの

- **v0.7.1 直交モード(`F8`)** — 実使用で「グリッドに頼らず水平・垂直な線を引きたい」から
- **v0.7.2 レイヤー重ね順(Front/Back)** — 実使用で「文字レイヤーを最前面、ハッチングを最背面にしたい」から

いずれも操作性・UI 側の痛点で、番号外リリースとして問題なく差し込めた。この方式が機能することの実証になっている(第1章の結論の根拠)。

---

## 1. 結論(先に読む用)

痛点を後で見つけたときの損失は**種類によって100倍くらい違う**。

| 痛点の種類 | 後で気づいた場合の損失 | 例 |
|---|---|---|
| 操作性・UI | **小**(app層に閉じ、いつ直しても同じ手間) | 直交モード、ショートカット、プレビュー、ボタン配置 |
| 足りないコマンド | **小〜中**(追加で済む。既存データに影響なし) | 矩形ツール、数値入力での作図、面取り、配列複製、尺度変更 |
| データモデル・意味論 | **大**(フォーマット移行 + 既存図面の見た目が変わる + DXF対応の再設計 + 出力コードの書き直し) | **単位系・尺度・用紙**、寸法スタイルの基準、線幅の意味、ハッチング、グループ/ブロック |

**v0.7.1(直交モード)・v0.7.2(レイヤー重ね順)は前者2つに当たり、M7完了後に番号外リリースとして問題なく差し込めた。この方式が機能することは実証済み。**
だから「操作性の痛点はM8を進めながら拾えばよい」。

一方で **M8に入る前に決めておく価値が明確に高いものがある: 単位系と尺度・用紙の扱い。**
理由は次章。

**(2026-08-01 改定)** 検討の結果、「M8前に決めるもの」は単位系を核に次の4点へ拡大した。
いずれも「決定」だけを前倒しし、実装は M8 以降でよい。詳細は第6章・第7章。

1. 単位系・尺度・用紙(従来どおり、2.1節)
2. 図面枠・紙空間の持ち方(6.1節)
3. 注記要素のサイズ基準と引出線(leader)の共通化(6.2節)
4. GPS 仕様を「意味として持つ」宣言と、製図規定・図面様式規定の策定(6.3節・第7章)

---

## 2. M8を先に進めた場合の損失(具体的な見積もり)

### 2.1 いちばん高い損失: 単位系・尺度・用紙を後から決める

現状 mcad には**単位の概念がない**。ワールド座標は無次元の f64 で、テキスト高さも寸法の矢印サイズも「ワールド単位」。DESIGN.md 第7章の随時対応にも `FIT_MIN_MARGIN` の項が「単位系確立後に見直す」として残っている(231行目)。M5 の移動量の設計判断でも「単位系未確立でスケール不定」が理由に挙がっている(252行目)。

M8 の中身は **SVG/PDF エクスポート・線幅の画面反映・設定永続化**。このうち出力と線幅は、単位と尺度が決まっていないと**意味が定まらない**:

- SVG/PDF に書くには「1ワールド単位 = 何 mm か」と「用紙上で 1:1 か 1:100 か」が必要
- 線幅 0.5 は 0.5mm か 0.5px か。ズームしても太さが変わらないのか、図面と一緒に拡大するのか
- 文字高さ 3.0 は「紙の上で 3mm」なのか「モデル空間で 3 単位」なのか。尺度が入ると両者は別物になる
- 寸法の矢印・文字も同じ問題を持つ

**単位を決めずにM8を作ると、暗黙の前提(たとえば 1単位 = 1mm、常に 1:1)で出力コードとテストを書くことになる。**
後から尺度・用紙を入れると、次が全部やり直しになる:

1. 出力の座標変換とテスト(SVG/PDF 両方)
2. 線幅・文字高さ・寸法要素の解釈 → **既存の `.mcad` 図面の見た目が変わる**ので、`.mcad` v4 + 移行規則が必要
3. 設定永続化のスキーマ(単位・既定尺度・用紙サイズが増える)
4. DXF の `$INSUNITS` 対応と往復テスト
5. README / DESIGN / CHANGELOG

**概算: M8 相当の作業をもう一度やる規模。** 逆に今決めておけば、M8 の設計に織り込むだけで追加コストはほぼゼロ。

### 2.2 二番目: 実使用の図面が「守るべき資産」になる前の猶予

DESIGN.md の v1.0.0 完成基準3は「`.mcad` フォーマットの後方互換を宣言できる」。
いま自分の図面は数枚しかないので、フォーマットの破壊的変更は「移行コードを書く手間」だけで済む。図面が増えて実務で使い始めると、同じ変更が「既存資産を壊さない移行 + 検証」に化ける。

**フォーマットに触る変更をするなら、今がいちばん安い。** 単位系・寸法スタイル・ハッチングはいずれもフォーマットに触る。

### 2.3 逆に、後回しでも損しないもの

- ショートカットの割当・モードの挙動・プレビュー・パネル配置(v0.7.1/v0.7.2 で実証済み)
- 足りない作図・編集コマンド(矩形、面取り、配列複製、尺度変更、数値入力での作図)
  - これらは既存データの意味を変えないので、いつ足しても同じ手間
  - ただし**数えられるほど溜まると「なぜ実用に耐えないか」の主因になる**ので、洗い出し自体は早いほうがよい
- 性能(M9 の空間インデックス)。要素数が実際に増えてから測るべきで、先に手を出す意味がない

---

## 3. 痛点を洗い出すためのアイデア

「触って気になったところをメモする」だと、慣れて回避策を覚えてしまい痛点が見えなくなる。
**サブシステムを狙い撃ちする図面を描く**のが効率的。以下は狙いと「見るべき点」つき。

### 優先度A: M8の設計入力になるので、M8着手前にやる価値が高い

#### A-1. 「出す」ことを前提に部品図1枚を仕上げる
実寸の部品(L字金具、穴あきプレート、ブラケット等)を寸法記入まで含めて1枚。
**紙に出したときにどう見えるべきかを意識して描く**のがポイント。

見るべき点:
- 用紙サイズと尺度をどうしたいか(A4 に 1:1? 1:2?)。**これがM8の要件そのものになる**
- 文字の大きさをどう決めたか。「紙で 3mm」で考えたか、画面で見た目を合わせたか
- 線の太さを使い分けたいか(外形線・寸法線・中心線)。今は画面に反映されない
- 寸法値の桁・単位表記(「120」か「120mm」か)
- 中心線・破線が必要になったか(線種は未実装)

#### A-2. 座標値の大きさを意図的に変える
同じ形を「100 x 50」で描いた図面と、「100000 x 50000」で描いた図面の2枚。

見るべき点:
- ズームフィット・スナップ許容量・グリッドが両方で自然に使えるか
- 寸法の文字や矢印が、図面の大きさに対して極端に大きく/小さくならないか
- **ここが破綻するなら、それは単位系が無いことの実害そのもの**

### 優先度B: 足りないコマンドの洗い出し(M8と並行でよい)

#### B-1. 建築平面図の一部(壁・開口部)
通り芯を引き、オフセットで壁の二重線を作り、開口部をトリムで抜く。

見るべき点:
- オフセット→トリムの往復が何クリックかかるか(壁1本あたり)
- ハッチング(壁の塗り)が無いことがどれだけ効くか
- 補助線(construction line)が欲しくならないか。今は通常の線をレイヤーで分けるしかない
- 矩形ツールが無いことの手間(今は Polyline で4点 + クローズ)

#### B-2. 繰り返しパターン(円周上のボルト穴、等間隔のフェンス)
円周に8個の穴、等間隔に並ぶ部材。

見るべき点:
- **配列複製(array)が無い**のを Ctrl+D + 回転で何回繰り返したか
- 正確な角度(45度きざみ)を指定する手段が無いこと(回転は3クリックの相対角)
- 極トラッキング(角度スナップ)が欲しくなったか。直交モードでは足りなかったか

#### B-3. 寸法を入れ直す/測る
描いた図に対して「この2点間は何mmか」を知りたい場面を作る。

見るべき点:
- 計測専用のコマンド(距離・面積)が無いこと。今は寸法を引いて消すしかない
- 直径寸法・角度寸法が無いこと(長さと半径のみ)
- 寸法の文字位置を後から動かしたくならないか(今は非関連静的寸法)

### 優先度C: 既存機能の耐久テスト

#### C-1. 他CAD由来のDXFを読んで加筆して書き戻す
LibreCAD 等で作った DXF を import → 編集 → export。

見るべき点:
- 色が基本9色に丸まる(既知の仕様)のが実用上どれだけ痛いか
- レイヤー重ね順が往復で失われる(既知)のが実害になるか
- 中央/右揃えの TEXT がスキップされる(既知)ものが実データに含まれていたか
- DIMENSION が import されない(M9予定)ことの実害

#### C-2. 保存 → 終了 → 再開 → 編集を数サイクル
1枚の図面を数日にわたって育てる想定。

見るべき点:
- 最近使ったファイルが無いこと(毎回ダイアログでパスを辿る)
- 起動ごとにスナップ・直交モード・グリッド間隔が既定へ戻ること(M8の設定永続化で解消予定)
- undo 履歴が読込でクリアされる仕様が邪魔にならないか
- レイヤーが増えたときパネルが破綻しないか(10枚、20枚)

#### C-3. 要素数を増やす(数百〜数千)
実図面でも意図的なコピー量産でもよい。

見るべき点:
- 描画・選択・スナップが体感で重くなる要素数(M9 の空間インデックスの根拠になる)
- 矩形選択で意図しないものを大量に拾ってしまわないか
- レイヤー非表示で軽くなるか

---

## 4. 痛点の記録の仕方(推奨フォーマット)

作業中は1行で済ませて、後でまとめて仕分ける。**頻度を必ず書く**のが要点 — 1回だけ困ったものは後回しでよく、毎回困るものは安くても先に直す価値がある。

```
[頻度] 何をしたかったか → 何が起きたか(回避方法)
例:
[毎回] 壁を200mmオフセットしたい → 距離入力欄はあるが方向をクリックで示す必要があり、
       真横に出したいのに斜めに出る(直交モードで回避できるが毎回F8を押す)
[1回]  円の直径寸法を入れたい → 半径寸法しかない(半径を入れて手で文字を直す、は不可)
```

仕分けの基準は第1章の表。**データモデルに触るものが出たら、そこで一度手を止めて設計を見直す価値がある。**

---

## 5. 推奨の進め方

0. **(2026-08-01 追加)製図規定.md の機能直結章(2様式・3尺度・4線・5寸法・6文字)を先に埋める**(第7章の方針で)。
   A-1 はこの規定に**従って**描くことで、規定と実装のギャップ洗い出しを兼ねる
1. **A-1(部品図1枚を出す前提で仕上げる)と A-2(座標スケール2枚)だけ先にやる。** 半日〜1日程度。
   - ここで「単位・尺度・用紙・線幅・文字高さをどうしたいか」の実感が得られる
   - A-1 では「図面枠を入れるつもりで描く」「表面性状記号・幾何公差を入れたい箇所に印をつける」も行う(6.1〜6.3節の実感を同時に取るため)
2. その実感を持って **M8 の設計に入る。** 単位系の確定を M8 の最初のタスクに含め、第6章の決定(図面枠・注記基準・GPS G0)と第7章の様式スキーマを設計入力とする
   - `FIT_MIN_MARGIN` の画面ピクセル基準化、`mcad-geom` の絶対EPS見直し(DESIGN.md 随時対応の2件)も、単位系確定と同時にやると一度で済む
3. **B・C 系の洗い出しは M8 と並行**でよい。出てきた操作性の痛点は v0.7.x の番号外リリースで差し込む(v0.7.1/v0.7.2 と同じ方式)

**やらないほうがよいこと**: 痛点を全部出し切ってから M8 に入る、という順序。操作性の痛点は無限に出るし、後から直しても損失が小さい。単位系だけ先に押さえれば、残りは走りながら拾えばよい。

---

## 6. M8前に決める設計入力(2026-08-01 追加)

単位系(2.1節)と同じく「後から変えるとフォーマット移行 + 出力コード書き直しになる」種類の決定。
**決定だけを M8 設計の入口で行い、実装は段階表に従って後送りしてよい。**

### 6.1 図面枠・紙空間の持ち方

図面枠は「用紙に対して常に 1:1」で、中身の図形には尺度がかかる。つまり紙空間(レイアウト)を
持つかどうかの決定そのもの。後から紙空間を足すと `.mcad` フォーマット・描画パイプライン・
SVG/PDF の座標変換・DXF(paper space / VIEWPORT)が全部やり直しになる。

**推奨: 単一モデル空間 + Document メタデータ(用紙・尺度)+ メタデータから生成する図面枠**で始める。
tcad が既にこの方式の実装事例(`tcad-kit` の `SheetMeta { scale, frame: Aabb }`、図枠は world 座標)。
フル紙空間(複数ビューポート・複数レイアウト)は v1.0.0 後の高度化とし、メタデータ方式なら
そこへの移行パスも「レイアウト1枚の特殊形」として説明できる。mcad が core 側に用紙メタデータを
持つ場合、tcad の `SheetMeta` との二重管理をどうするかを設計時に確認する(tcad は mcad 都合で
変更しない前提のため、mcad 側は tcad が無視できる形にする)。

### 6.2 注記要素のサイズ基準と引出線(leader)の共通化

- **サイズ基準**: JIS/ISO では文字高さ・記号各部の寸法は**紙面上の mm** で定義される
  (文字高さは呼び系列 2.5 / 3.5 / 5 / 7 / 10 …。製図規定.md 第6章も「呼び3.5を標準」)。
  尺度導入後もこれが成り立つよう、**注記系エンティティ(Text・寸法・将来の記号)のサイズは
  紙基準 mm で持つ**と決める。現在の「ワールド単位」のままだと尺度を変えるだけで記号・文字が壊れ、
  後からの変更は `.mcad` バージョン上げ + 既存図面の見た目変更 + 移行規則になる。
- **leader の共通化**: 現在は `DimRadial` が `leader_angle: f64` を内部に埋め込むのみ。
  溶接記号・表面性状・データム・幾何公差・バルーン注記はすべて引出線を使うため、
  **引出線を独立要素にするか各記号に埋めるかを記号実装前に決める**。記号を1つ作った後だと変更コストが跳ねる。

### 6.3 GPS(幾何特性仕様)準拠 — 「意味として持つ」宣言

GPS 準拠の本質は記号の形ではなく、**仕様が構造化データとして表現されていること**。

| 方式 | 保持するもの | できること |
|---|---|---|
| 絵として持つ | 線分と文字列 | 描ける。以上 |
| 意味として持つ | 特性種別・公差値・公差域・修飾子・データム系 | 描ける + 文法検証 + 再フォーマット + tcad の充足チェック入力 |

後から意味論へ移行するのは実質不可能(文字列の再パース)。逆に意味を持てば絵はいつでも生成できる。
**記号を1つでも実装する前に「意味として持つ」と宣言しておく。** tcad の「検図=充足チェック」にとって、
構造化された GPS 仕様は入力そのものであり、役割A(geom/core が tcad の土台)にも効く。

**段階表**:

| 段階 | 内容 | 時期 |
|---|---|---|
| **G0: 決定のみ** | ①意味として持つ宣言 ②Document 図面メタデータ(単位・尺度・用紙・準拠規格・一般公差)③線幅・文字高さの基準(第7章)④注記は紙基準(6.2節)⑤記号はジオメトリ生成(下記)⑥`EntityGeom` へ `#[non_exhaustive]` 付与 | **M8 設計に含める** |
| G1: 基盤 | 引出線エンティティ、注記の紙基準レンダリング、用紙 + 表題欄(第7章) | M8 内 or 直後 |
| G2: 寸法の GPS 化 | サイズ公差 + 修飾子(JIS B 0420 / ISO 14405 系)を寸法に載せる。`.mcad` バージョン上げ | M8 直後。**M9 の DXF DIMENSION import より前**(後だと二度手間) |
| G3: 記号本体 | 幾何公差記入枠 + データム → 表面性状 → 溶接記号 | M9 以降 / 番号外。順序は依存関係(データムは公差枠が前提、溶接は独立) |

**設計上の留意点**:

- **記号はフォントに頼らずジオメトリとして生成する**。⌀ ⌖ Ⓜ 等を持つフォントはほぼ無く、
  egui の ASCII 制約とも衝突する。JIS/ISO は記号各部を線幅 d・文字高さ h の比で定義しており関数で描ける。
  `mcad-geom` に「記号 → パス」の純関数群を置けば依存方向にも合致し、DXF export の分解書き出し・
  SVG/PDF 出力でも同じ生成器を使い回せる。
- **DXF の非対称**: 溶接・表面性状・データム記号は DXF に専用エンティティが無く、実 CAD は
  ブロック + 線分・文字に分解して書く。mcad が core エンティティとして持つと
  「export は分解できるが import は復元不能」の非対称が生じる(レイヤー重ね順と同種の仕様として非保存)。
- **`#[non_exhaustive]`**: 現在 `EntityGeom` に付いておらず、M6 のバリアント追加時に tcad 側で
  無警告のデータ欠落リスクが生じた実績がある(`tcad-kit/src/snapshot.rs` のコメント参照)。
  GPS 記号のバリアント追加で同じことを繰り返す前に付与を検討する(tcad 側の match への影響確認込み)。
- **non-goal を先に切る**: mcad が引き受けるのは**仕様(specification)側のみ**。
  やる = 構造化保持・作図・出力・文法検証(例: 真円度にデータム指定を弾く)。
  やらない = 公差解析(スタックアップ)・実測データ評価・完全な MBD(ISO 16792)。
  ここを文書化しないと「GPS 準拠」は際限なく膨らむ。
- 規格の版は実装時に手元で要確認(表面性状は ISO 21920 系への移行期、一般公差は ISO 22081 への移行期。
  AI の知識だけで規格番号を確定させない)。
- GPS を本気で進めると v1.0.0 の完成基準「1枚仕上げて出せる」から重心がずれる。
  **G0 の決定だけ v1.0.0 前に打ち、G2/G3 は v1.0.0 後に回してよい**(G2 のみ M9 DXF との順序制約あり)。

---

## 7. 製図規定・図面様式規定の方針(2026-08-01 追加)

ルートの [`製図規定.md`](./製図規定.md) は「このリポジトリで作成する図面」の内容規定であり、
mcad にとっては**要求仕様の源泉**として扱う。規定が要求する線種・記号・様式のうち mcad が
未対応のものが、そのまま機能ギャップ一覧になる(痛点洗い出し A 系の答え合わせにも使う)。

### 7.1 製図規定の充実方針

- **準拠体系**: JIS B 0001 を親規格とし、GPS 系 JIS(B 0024=ISO 8015、B 0021=ISO 1101、
  B 0022=ISO 5459、B 0420 系=ISO 14405 系、B 0031=表面性状、Z 3021=溶接記号)と整合させる。
  規定の各章に対応 JIS/ISO 番号を明記する(版は執筆時に要確認)。
- **空欄を埋める優先順位**: mcad の機能に直結する章を先に —
  **2(図面様式)・3(尺度)・4(線)・5(寸法)・6(文字)が先**、作図法の章
  (7 投影法・8 断面図・9 省略・10 加工部)は作図者向けで機能要件が薄いため後回し。
- **線(第4章)**: mcad の線幅は **ISO 128 の√2系列(0.13 / 0.18 / 0.25 / 0.35 / 0.5 / 0.7 /
  1.0 / 1.4 / 2.0 mm)の列挙を基本とする案を維持**する。規定 4-1 の線幅表はあくまで参考であり、
  系列に乗らない値(0.60 / 0.40 / 0.15 / 0.20)は規定側を系列値へ寄せる詰め調整を行う
  (候補: 0.60→0.7、0.40→0.35、0.15→0.13、0.20→0.25。0.18 は系列内)。太線:細線 = 2:1 の
  組(0.7/0.35 等)を保つよう線幅グループ単位で選ぶ。規定の「実線_太」等の名称は
  名前付きスタイル(スタイル名 → 線種 + 系列内線幅)として実装する想定。任意 mm 値の許容が
  必要になるかは M8 設計時に判断する。また規定は一点鎖線・二点鎖線・破線を必須で使うため、
  **未実装の線種の優先度が上がる**(M8 の線幅画面反映と同時に線種も設計する)。
- **文字(第6章)**: 呼び系列 + 基準枠高さ h は JIS Z 8313 / ISO 3098 と整合。図面内 CJK は
  M6 の Noto Sans JP フォールバック(`mcad-app/src/fonts.rs`)で対応済み。文字高さは
  紙基準 mm + 呼び系列(6.2節の決定と同一)。
- **寸法(第5章)**: 5-3 の寸法補助記号表(φ・Sφ・□・R・CR・SR …)を埋める際、
  M6 実装済み寸法の接頭辞・表記と突き合わせ、未対応記号を機能ギャップとして記録する。
- **規定の版管理**: 規定自体に版番号を持たせ、Document の図面メタデータ(G0-②)に
  「準拠規定の版」を記録できるようにする。
- 誤記は 2026-08-01 にユーザーが訂正済み。
  `製図規定.md` は現在 git 未追跡(`A-*.md` と違い gitignore 対象外)。
  AGENTS.md と同様のエージェント向け規約集なのでコミットして版管理する想定だが、ユーザー判断による。

### 7.2 図面様式規定(規定第2章)の策定方針

図面様式は 6.1 節「紙空間の持ち方」の要件そのもの。**様式も GPS と同じ原則で「意味として持つ」**
(様式 = Document メタデータのスキーマ + そこから生成される枠。枠を手描き図形のテンプレート
貼り付けにしない)。表題欄の項目定義がそのまま Document メタデータのスキーマになり、
M8 の設定永続化・SVG/PDF 出力の直接入力になる。

規定側で決める項目:

1. **用紙**: JIS Z 8311 の A 系列。当面の実用は A4 / A3(横)中心。輪郭線・余白
   (綴じ代含む)・中心マーク等の要否
2. **表題欄**: 項目(図面番号・名称・尺度・投影法・作成者・日付・改訂欄等、ISO 7200 参照)と配置
3. **尺度系列**: JIS Z 8314 の推奨系列から使うものを列挙(規定第3章と共通)
4. **標準レイヤー構成**: 規定 4-1 の線名称とレイヤー/スタイルの対応
   (現状の既定レイヤーセット `"0"` / `"Text"` を規定側から見直す)

---

## 付録: 現時点で分かっている「無いもの」

洗い出しの答え合わせに使う。これらは既に把握済みなので、**これ以外**が出てきたら収穫。

- 作図: 矩形・正多角形・楕円・スプライン・線種(破線/中心線)・ハッチング・グループ/ブロック
- 編集: 面取り(chamfer)・配列複製(array)・尺度変更(scale)・ストレッチ・グリップ編集(ハンドルドラッグ)
- 入力: 長さ・角度の数値入力による作図(入力欄はオフセット距離・フィレット半径・文字高さのみ)、極トラッキング(角度スナップ)
- 寸法: 直径寸法・角度寸法・寸法スタイル・関連寸法(図形変更に追従)
- 計測: 距離・面積の計測コマンド
- 出力: SVG/PDF・印刷・用紙/尺度の概念(M8予定)
- 画面: 線幅の反映(M8予定)
- 設定: 永続化・最近使ったファイル(M8予定)
- 基盤: 単位系・空間インデックス(M9予定)・幾何拘束ソルバー(M10予定)
- DXF: DIMENSION import(M9予定)・真のRGB(拡張色420、未調査)・レイヤー重ね順(仕様として非保存)
- 様式・注記(2026-08-01 追加、第6・7章): 図面枠・表題欄・用紙メタデータ・名前付き線スタイルテーブル・
  引出線(独立要素として)・寸法公差と ISO 14405 修飾子・幾何公差記入枠・データム記号・
  表面性状記号・溶接記号・寸法補助記号の入力支援(φ・□・SR 等)
